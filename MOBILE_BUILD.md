# Инструкция по сборке мобильного приложения BomberFPV

## Подготовка для Android

### Требования

1. **Python 3.8+**
2. **Buildozer** - инструмент для сборки Android приложений
3. **Android SDK и NDK** (автоматически загружаются Buildozer)
4. **Java JDK** (для подписи APK)

### Установка Buildozer

```bash
# Linux / macOS
pip install buildozer

# Windows (рекомендуется использовать WSL или Linux VM)
# Buildozer не работает нативно на Windows
```

### Настройка для Android

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Убедитесь, что файл `buildozer.spec` настроен правильно.

3. Соберите APK:
```bash
buildozer android debug
```

Для release версии (требует подписи):
```bash
buildozer android release
```

### Структура проекта для мобильной сборки

```
BomberFPV/
├── main.py              # Точка входа для мобильного приложения
├── buildozer.spec       # Конфигурация Buildozer
├── src/
│   ├── game.py          # Основная игра
│   └── sounds.py         # Система звуков
├── sounds/              # Папка для звуковых файлов (опционально)
└── requirements.txt     # Зависимости Python
```

### Особенности мобильной версии

1. **Сенсорное управление:**
   - Виртуальный джойстик в левом нижнем углу для управления дроном
   - Кнопка бомбы (💣) в правом нижнем углу
   - Кнопка апгрейдов (⚙) в правом верхнем углу

2. **Адаптивный интерфейс:**
   - Автоматическое определение мобильного устройства
   - Портретная ориентация экрана
   - Оптимизированные размеры элементов управления

3. **Звуки:**
   - Автоматическая генерация звуков, если файлы отсутствуют
   - Поддержка загрузки пользовательских звуковых файлов из папки `sounds/`

### Отладка на Android

1. Подключите устройство через USB с включенной отладкой
2. Установите APK:
```bash
buildozer android deploy
```

3. Просмотр логов:
```bash
buildozer android logcat
```

### Решение проблем

**Проблема:** Buildozer не работает на Windows
**Решение:** Используйте WSL2 (Windows Subsystem for Linux) или Linux VM

**Проблема:** Ошибки при сборке
**Решение:** 
- Убедитесь, что все зависимости установлены
- Проверьте версию Python (должна быть 3.8+)
- Очистите кеш: `buildozer android clean`

**Проблема:** Приложение не запускается на устройстве
**Решение:**
- Проверьте минимальную версию Android (API 21+)
- Убедитесь, что устройство поддерживает архитектуру (arm64-v8a или armeabi-v7a)

### Альтернативные методы сборки

#### Использование Python-for-Android напрямую

```bash
pip install python-for-android
p4a create --requirements=python3,pygame --arch=arm64-v8a
```

#### Использование BeeWare (для iOS и Android)

```bash
pip install briefcase
briefcase create android
briefcase build android
briefcase run android
```

## iOS (экспериментально)

Для iOS требуется macOS и Xcode. Используйте BeeWare или pyobjus.

## Тестирование на эмуляторе

1. Установите Android Studio
2. Создайте эмулятор
3. Запустите эмулятор
4. Используйте `buildozer android deploy` для установки

## Публикация в Google Play

1. Соберите release APK/AAB
2. Подпишите приложение
3. Загрузите в Google Play Console
4. Заполните метаданные и скриншоты
